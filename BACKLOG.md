# Backlog - План доработок бота Санечек

## Легенда статусов
- ✅ **Завершено** - реализовано и задеплоено
- 🔄 **В работе** - в процессе разработки
- 📋 **Запланировано** - в очереди на реализацию
- 🔴 **Критично** - требует немедленного внимания

---

## 🐛 Баги из текущего тестирования

*Баги, обнаруженные при ручном тестировании. Будут исправляться по мере поступления.*

**План работы:**
1. ✅ Собрать список всех багов
2. 🔄 Изучить причины каждого бага (в процессе)
3. 📋 Спроектировать решения
4. 📋 Реализовать исправления

---

## 🔍 Анализ причин багов

### Общие проблемы архитектуры

#### Проблема #1: Порядок обработчиков и ConversationHandler
**Затронутые баги:** #3, #4, #6, #7, #10

**Корневая причина:**
В `handlers/base.py` ConversationHandler для `/task` (строка 69) добавлен БЕЗ указания группы (по умолчанию group=0), и он может перехватывать команды, которые должны обрабатываться другими CommandHandler.

**Детали:**
- ConversationHandler проверяет `entry_points` только когда пользователь НЕ в состоянии
- Но если пользователь в состоянии (например, TASK_TEXT), то все сообщения обрабатываются через `states`
- В `receive_task_text()` есть проверка на команды (строка 760), которая завершает ConversationHandler
- Проблема: когда пользователь НЕ в состоянии ConversationHandler, но отправляет команду `/summary`, ConversationHandler может перехватывать её, если она не обработана fallbacks

**Почему это происходит:**
- В python-telegram-bot обработчики обрабатываются в порядке добавления
- ConversationHandler добавлен ПЕРЕД CommandHandler для других команд (строки 69-71 vs 95-101)
- Если ConversationHandler активен (пользователь в состоянии), он имеет приоритет
- Но даже когда пользователь НЕ в состоянии, ConversationHandler может проверять entry_points и перехватывать команды

---

### Детальный анализ каждого бага

#### Баг #1: Вопрос через @bot не работает
**Файл:** `handlers/mention_handler.py`, строка 496

**Корневая причина:**
Используется несуществующее поле `Message.timestamp` вместо `Message.created_at`.

**Детали:**
- В модели `Message` (database/models.py) есть только поле `created_at` (строка 237)
- В функции `_get_user_message_context()` используется `Message.timestamp` (строка 496)
- Это вызывает AttributeError при попытке получить контекст сообщений
- Ошибка перехватывается общим `except Exception` в `mention_handler()` (строка 676-678)

**Решение:**
Заменить `Message.timestamp` на `Message.created_at`.

---

#### Баг #2: /edit не определяет время
**Файлы:** `handlers/tasks.py`, `handlers/reply_analyzer.py`

**Корневая причина:**
При редактировании задачи через `/edit` с реплаем, система не определяет, что "16:00" - это время для дедлайна, а не новый текст задачи.

**Детали:**
- Функция `edit_handler()` определяет, что редактировать, на основе контекста реплая
- Если реплай на задачу, вызывается `_process_inline_edit()` или `receive_edit_value()`
- В `receive_edit_value()` нет логики определения типа редактирования (текст/дедлайн/исполнитель)
- Система по умолчанию редактирует текст задачи

**Решение:**
Добавить логику определения типа редактирования:
- Если значение похоже на время (например, "16:00", "завтра в 15:00") → редактировать дедлайн
- Если значение начинается с "@" → редактировать исполнителя
- Иначе → редактировать текст

---

#### Баги #3, #7, #10: Команды перехватываются /task
**Файл:** `handlers/base.py`

**Корневая причина:**
ConversationHandler для `/task` перехватывает команды `/summary`, `/subscribe`, `/reminders`, `/setadmin` когда пользователь находится в состоянии ConversationHandler.

**Детали:**
- ConversationHandler для `/task` добавлен на строке 69 БЕЗ указания группы (group=0 по умолчанию)
- CommandHandler для других команд добавлены на строках 95-101, тоже без группы (group=0)
- Когда пользователь в состоянии ConversationHandler (например, TASK_TEXT), все сообщения обрабатываются через states
- В `receive_task_text()` есть проверка на команды (строка 760), но она работает только для выхода из ConversationHandler
- Проблема: если пользователь НЕ в состоянии, но ConversationHandler все равно проверяет entry_points и может перехватить команду

**Решение:**
1. Убедиться, что fallbacks в ConversationHandler правильно обрабатывают все команды
2. Или изменить порядок: добавить CommandHandler ПЕРЕД ConversationHandler
3. Или добавить фильтр в ConversationHandler, чтобы он не перехватывал другие команды

---

#### Баг #4: /task не принимает текст задачи
**Файлы:** `handlers/base.py`, `handlers/tasks.py`, `handlers/intent_router.py`

**Корневая причина:**
Сообщения в ConversationHandler перехватываются другими обработчиками (intent_router, reply_to_bot_handler) ДО того, как попадают в ConversationHandler.

**Детали:**
- `intent_router_handler` зарегистрирован с group=-1 (строка 111-114)
- `reply_to_bot_handler` зарегистрирован с group=-2 (строка 129-132)
- Эти обработчики выполняются РАНЬШЕ ConversationHandler (group=0)
- Когда пользователь в состоянии TASK_TEXT и отправляет текст, эти обработчики могут перехватить сообщение
- Фильтр `~filters.COMMAND` не помогает, так как текст задачи - это не команда

**Решение:**
1. Добавить проверку в `intent_router_handler` и `reply_to_bot_handler`: если пользователь в состоянии ConversationHandler, пропустить обработку
2. Или изменить группы: ConversationHandler должен иметь более высокий приоритет

---

#### Баг #5: /done не работает без реплая
**Файл:** `handlers/tasks.py`, функция `done_handler()` (строка 1357)

**Корневая причина:**
Функция `done_handler()` требует реплай на сообщение с задачей (строка 1359-1361). Если реплая нет, бот отвечает "Ответь на сообщение с задачей".

**Детали:**
- Это может быть по дизайну, но UX можно улучшить
- Можно добавить логику: если реплая нет, показать список активных задач пользователя для выбора

**Решение:**
Добавить fallback логику: если реплая нет, показать список активных задач пользователя в этом чате с кнопками для выбора.

---

#### Баг #6: /cost перехватывается другими обработчиками
**Файлы:** `handlers/base.py`, `handlers/expenses.py`, `handlers/ask.py`

**Корневая причина:**
Аналогично багу #4 - сообщения в ConversationHandler для `/cost` перехватываются `intent_router_handler` и `reply_to_bot_handler`.

**Детали:**
- ConversationHandler для `/cost` добавлен на строке 71
- Когда пользователь в состоянии COST_AMOUNT или COST_DESCRIPTION, отправляет текст (например, "500")
- `intent_router_handler` (group=-1) или `reply_to_bot_handler` (group=-2) перехватывают сообщение
- Также ConversationHandler для `/cost` может работать неправильно - спрашивает про исполнителя и дедлайн (это для задач, не для расходов)

**Решение:**
1. Добавить проверку в `intent_router_handler` и `reply_to_bot_handler`: если пользователь в состоянии ConversationHandler, пропустить обработку
2. Проверить логику ConversationHandler для `/cost` - почему спрашивает про исполнителя и дедлайн

---

#### Баг #8: /mytasks не работает в личном чате
**Файл:** `handlers/tasks.py`, функция `mytasks_handler()` (строка 1257)

**Корневая причина:**
В функции есть обработка личных чатов (строки 1290-1349), но ошибка возникает где-то в процессе выполнения.

**Детали:**
- Функция обернута в try-except (строка 1259)
- При ошибке бот отвечает "❌ Чёт не вышло показать задачи. Попробуй позже."
- Нужно проверить логи для понимания конкретной ошибки
- Возможные причины: ошибка при запросе к БД, ошибка при форматировании, ошибка при отправке сообщения

**Решение:**
1. Улучшить обработку ошибок: логировать конкретную ошибку
2. Проверить запросы к БД для личных чатов
3. Проверить форматирование сообщений

---

#### Баг #9: Текст задачи обрезается при создании
**Файлы:** `handlers/intent_router.py`, `llm/intent_prompts.py`

**Корневая причина:**
LLM в `intent_router` неправильно извлекает текст задачи из сообщения, обрезая важную часть.

**Детали:**
- В `intent_router.py` используется LLM для извлечения данных задачи (строка 141-200)
- LLM возвращает JSON с полем `task_text`
- Проблема: LLM может обрезать текст, оставляя только ключевые слова
- В `task_detector.py` (команда `/detect`) используется другой промпт, который работает правильно

**Решение:**
1. Сравнить промпты в `intent_router` и `task_detector`
2. Улучшить промпт в `intent_router`: явно указать, что нужно сохранять весь текст задачи
3. Добавить fallback: если LLM обрезал текст, использовать исходное сообщение

---

#### Проблема #2: Перехват сообщений другими обработчиками
**Затронутые баги:** #4, #6

**Корневая причина:**
Обработчики с отрицательными группами (intent_router group=-1, reply_to_bot_handler group=-2) могут перехватывать сообщения, которые должны обрабатываться ConversationHandler.

**Детали:**
- `intent_router_handler` зарегистрирован с group=-1 (строка 111-114)
- `reply_to_bot_handler` зарегистрирован с group=-2 (строка 129-132)
- Эти обработчики выполняются РАНЬШЕ, чем ConversationHandler (group=0)
- Когда пользователь в состоянии ConversationHandler и отправляет текст, эти обработчики могут перехватить сообщение ДО того, как оно попадет в ConversationHandler

---

### 📋 Баг #1: Вопрос к боту через упоминание (@bot) не работает
**Приоритет:** 🔴 Критично  
**Статус:** 📋 В очереди на анализ

**Описание проблемы:**
При упоминании бота с вопросом (например, `@bot где тут ошибка?`) бот отвечает "❌ Чёт облажался. Попробуй ещё раз.", хотя через команду `/ask` всё работает корректно.

**Пример воспроизведения:**
```
Пользователь: @sanya_sanechka_bot где тут ошибка?
print("Hello World')
Бот: ❌ Чёт облажался. Попробуй ещё раз.

Пользователь: /ask найди тут ошибку
print("Hello World')
Бот: print("Hello World'") — лишняя одинарная кавычка... ✅ Работает
```

**Шаги для анализа:**
- [ ] Изучить обработчик упоминаний `mention_handler.py`
- [ ] Сравнить с обработчиком `/ask` в `ask.py`
- [ ] Проверить логику определения вопросов
- [ ] Найти место, где возникает ошибка
- [ ] Изучить обработку ошибок

**Файлы для изучения:**
- `handlers/mention_handler.py`
- `handlers/ask.py`
- `handlers/base.py` (регистрация обработчиков)

---

### 📋 Баг #2: /edit не определяет время, редактирует текст вместо дедлайна
**Приоритет:** 🔴 Критично  
**Статус:** 📋 В очереди на анализ

**Описание проблемы:**
При использовании `/edit` для изменения времени задачи (например, "16:00"), вместо изменения дедлайна редактируется текст задачи.

**Пример воспроизведения:**
```
Пользователь: @sanya_sanechka_bot заведи на завтра встречу
Бот: ✅ Готово, задача создана.
     📌 заведи на завтра встречу
     👤 @murzagalin
     📅 27.01.2026 в 00:01

Пользователь: /edit 16:00 (реплай на задачу)
Бот: ✏️ Задача изменена: "16:00"
     📝 Текст: "16:00"  ❌ Неправильно - должен изменить время дедлайна
     👀 @murzagalin, обрати внимание
```

**Ожидаемое поведение:**
- Дедлайн должен измениться на завтра в 16:00
- Текст задачи должен остаться "заведи на завтра встречу"

**Шаги для анализа:**
- [ ] Изучить обработчик `/edit` в `handlers/tasks.py`
- [ ] Проверить логику определения, что редактировать (текст/дедлайн/исполнитель)
- [ ] Изучить парсинг времени в `utils/date_parser.py`
- [ ] Проверить обработку реплаев на задачи
- [ ] Найти место, где определяется тип редактирования

**Файлы для изучения:**
- `handlers/tasks.py` (функции редактирования)
- `handlers/reply_analyzer.py` (анализ реплаев)
- `utils/date_parser.py` (парсинг времени)
- `utils/intent_helpers.py` (определение намерений)

---

### 📋 Баг #3: /summary обрабатывается как /task
**Приоритет:** 🔴 Критично  
**Статус:** 📋 В очереди на анализ

**Описание проблемы:**
Команда `/summary` (с упоминанием бота или без) обрабатывается как `/task`, вместо того чтобы показать саммари переписки.

**Пример воспроизведения:**
```
Пользователь: /summary@sanya_sanechka_bot
Бот: А что делать-то надо? Напиши текст задачи.  ❌ Неправильно
```

**Ожидаемое поведение:**
- Должен показать саммари переписки за сегодня
- Или предложить подписаться на саммари, если подписки нет

**Шаги для анализа:**
- [ ] Изучить обработчик `/summary` в `handlers/summary.py`
- [ ] Проверить регистрацию обработчиков в `handlers/base.py`
- [ ] Проверить порядок обработчиков (возможно, ConversationHandler для `/task` перехватывает)
- [ ] Проверить обработку команд с упоминанием бота (`/command@botname`)
- [ ] Найти, почему `/summary` попадает в обработчик задач

**Файлы для изучения:**
- `handlers/summary.py`
- `handlers/tasks.py` (ConversationHandler для /task)
- `handlers/base.py` (порядок регистрации обработчиков)

---

### 📋 Баг #4: Команда /task не работает - не принимает текст задачи
**Приоритет:** 🔴 Критично  
**Статус:** 📋 В очереди на анализ

**Описание проблемы:**
Команда `/task` входит в режим создания задачи, но не принимает текст задачи в следующем сообщении. Пользователь может ввести текст, но задача не создается.

**Пример воспроизведения:**
```
Пользователь: /task@sanya_sanechka_bot
Бот: Что нужно сделать? Укажи ответным сообщением

Пользователь: /task встретиться 24.01 в 16:00
Бот: (нет ответа или не обрабатывает)

Пользователь: /task встретиться 27.01 в 16:00
Бот: (нет ответа или не обрабатывает)

Пользователь: /task@sanya_sanechka_bot
Бот: А что делать-то надо? Напиши текст задачи.
```

**Ожидаемое поведение:**
- После `/task` бот должен принять текст задачи в следующем сообщении
- Если текст задачи указан сразу в команде `/task текст`, должен обработать сразу
- Должен правильно парсить дату и время из текста

**Шаги для анализа:**
- [ ] Изучить ConversationHandler для `/task` в `handlers/tasks.py`
- [ ] Проверить состояния ConversationHandler (TASK_TEXT, TASK_ASSIGNEE, TASK_DEADLINE)
- [ ] Проверить обработку текста задачи в `receive_task_text()`
- [ ] Проверить, не перехватывают ли другие обработчики сообщения
- [ ] Проверить порядок обработчиков в `handlers/base.py`
- [ ] Проверить обработку команд с аргументами `/task текст`
- [ ] Проверить обработку команд с упоминанием `/task@botname`

**Файлы для изучения:**
- `handlers/tasks.py` (ConversationHandler и функции обработки)
- `handlers/base.py` (порядок регистрации обработчиков)
- `handlers/intent_router.py` (возможно перехватывает сообщения)

---

### 📋 Баг #5: Команда /done не работает без реплая
**Приоритет:** 🔴 Критично  
**Статус:** 📋 В очереди на анализ

**Описание проблемы:**
Команда `/done` не работает, если не используется реплай на сообщение с задачей. Бот отвечает "Это не задача. Ответь на сообщение с задачей", даже если в чате есть активные задачи пользователя.

**Пример воспроизведения:**
```
Пользователь: @sanya_sanechka_bot заведи на завтра встречу
Бот: ✅ Готово, задача создана.
     📌 заведи на завтра встречу
     👤 @murzagalin
     📅 27.01.2026 в 00:01

Пользователь: /done@sanya_sanechka_bot
Бот: Это не задача. Ответь на сообщение с задачей  ❌ Не работает
```

**Ожидаемое поведение:**
- `/done` без реплая должен закрывать последнюю активную задачу пользователя в этом чате
- Или показывать список активных задач для выбора
- Или работать только с реплаем (но тогда нужно улучшить UX)

**Шаги для анализа:**
- [ ] Изучить обработчик `/done` в `handlers/tasks.py`
- [ ] Проверить логику определения задачи из реплая
- [ ] Проверить, есть ли логика для `/done` без реплая
- [ ] Проверить обработку команд с упоминанием бота (`/done@botname`)
- [ ] Изучить, как определяется задача из контекста сообщения
- [ ] Проверить, сохраняется ли `confirmation_message_id` при создании задачи

**Файлы для изучения:**
- `handlers/tasks.py` (функция `done_handler`)
- `database/models.py` (модель Task, поля `confirmation_message_id`)

---

### 📋 Баг #6: Команда /cost перехватывается другими обработчиками, отправляет дублирующие сообщения
**Приоритет:** 🔴 Критично  
**Статус:** 📋 В очереди на анализ

**Описание проблемы:**
Команда `/cost` перехватывается обработчиком `/ask` (или intent_router), из-за чего бот отправляет по 2 сообщения. Также ConversationHandler для `/cost` работает неправильно - спрашивает про исполнителя и дедлайн, хотя должен спрашивать только про описание расхода.

**Пример воспроизведения:**
```
Пользователь: /cost@sanya_sanechka_bot
Бот: Какая сумма?

Пользователь: 500
Бот: 500 чего? Ты конкретику давай...  ❌ Перехвачено /ask
     Осталось на сегодня: 8
Бот: Кто исполнитель? Укажи ответным сообщением @username  ❌ Неправильно для /cost

Пользователь: @murzagalin
Бот: Привет, что за вопрос? Давай...  ❌ Перехвачено /ask
     Осталось на сегодня: 7
Бот: Какой дедлайн? (например: завтра, в пятницу, 15.02)  ❌ Неправильно для /cost

Пользователь: завтра
Бот: Что тебе нужно сделать завтра? Конкретизируй задачу.  ❌ Перехвачено /ask
     Осталось на сегодня: 6
```

**Ожидаемое поведение:**
- `/cost` должен спрашивать только сумму и описание расхода
- Не должен спрашивать про исполнителя и дедлайн (это для задач)
- Сообщения в ConversationHandler не должны перехватываться другими обработчиками

**Шаги для анализа:**
- [ ] Изучить ConversationHandler для `/cost` в `handlers/expenses.py`
- [ ] Проверить порядок обработчиков в `handlers/base.py`
- [ ] Проверить, не перехватывает ли `intent_router_handler` сообщения в ConversationHandler
- [ ] Проверить, не перехватывает ли `reply_to_bot_handler` сообщения в ConversationHandler
- [ ] Проверить состояния ConversationHandler для `/cost` (COST_AMOUNT, COST_DESCRIPTION)
- [ ] Убедиться, что ConversationHandler имеет приоритет над другими обработчиками
- [ ] Проверить логику ConversationHandler - почему спрашивает про исполнителя и дедлайн

**Файлы для изучения:**
- `handlers/expenses.py` (ConversationHandler для /cost)
- `handlers/base.py` (порядок регистрации обработчиков, группы)
- `handlers/intent_router.py` (возможно перехватывает)
- `handlers/ask.py` (reply_to_bot_handler)

---

### 📋 Баг #7: Команды /subscribe и /reminders перехватываются /task
**Приоритет:** 🔴 Критично  
**Статус:** 📋 В очереди на анализ

**Описание проблемы:**
Команды `/subscribe` и `/reminders` (с упоминанием бота или без) перехватываются ConversationHandler для `/task`, из-за чего бот отвечает "А что делать-то надо? Напиши текст задачи." вместо выполнения команды.

**Пример воспроизведения:**
```
Пользователь: /subscribe@sanya_sanechka_bot
Бот: А что делать-то надо? Напиши текст задачи.  ❌ Неправильно

Пользователь: /reminders@sanya_sanechka_bot
Бот: А что делать-то надо? Напиши текст задачи.  ❌ Неправильно
```

**Ожидаемое поведение:**
- `/subscribe` должен показать меню подписок на саммари
- `/reminders` должен показать список активных напоминаний
- Не должны перехватываться ConversationHandler для `/task`

**Связь с другими багами:**
- Похожая проблема с багом #3 (`/summary` тоже перехватывается)
- Общая проблема: ConversationHandler для `/task` слишком агрессивно перехватывает команды

**Шаги для анализа:**
- [ ] Изучить порядок регистрации обработчиков в `handlers/base.py`
- [ ] Проверить, почему ConversationHandler для `/task` перехватывает другие команды
- [ ] Проверить фильтры в ConversationHandler для `/task`
- [ ] Изучить обработчики `/subscribe` и `/reminders`
- [ ] Проверить, правильно ли зарегистрированы CommandHandler для этих команд
- [ ] Убедиться, что CommandHandler регистрируются ДО ConversationHandler

**Файлы для изучения:**
- `handlers/base.py` (порядок регистрации обработчиков)
- `handlers/tasks.py` (ConversationHandler для /task, фильтры)
- `handlers/summary.py` (обработчик /subscribe)
- `handlers/reminders.py` (обработчик /reminders)

---

### 📋 Баг #8: Команда /mytasks не работает в личном чате
**Приоритет:** 🔴 Критично  
**Статус:** 📋 В очереди на анализ

**Описание проблемы:**
Команда `/mytasks` не работает в личном чате (private chat). Бот отвечает "❌ Чёт не вышло показать задачи. Попробуй позже." вместо показа задач пользователя.

**Пример воспроизведения:**
```
Пользователь: /mytasks (в личном чате с ботом)
Бот: ❌ Чёт не вышло показать задачи. Попробуй позже.  ❌ Не работает
```

**Ожидаемое поведение:**
- `/mytasks` в личном чате должен показать все задачи пользователя из всех чатов
- Или показать задачи по чатам с группировкой
- Не должен выдавать ошибку

**Связь с другими багами:**
- В BACKLOG есть запись о том, что этот баг был исправлен ранее, но похоже, что проблема вернулась или исправление было неполным

**Шаги для анализа:**
- [ ] Изучить обработчик `/mytasks` в `handlers/tasks.py`
- [ ] Проверить обработку личных чатов (chat.type == "private")
- [ ] Проверить логику получения задач пользователя
- [ ] Проверить обработку ошибок в функции `mytasks_handler`
- [ ] Проверить, правильно ли работает запрос к БД для личных чатов
- [ ] Проверить логи для понимания конкретной ошибки

**Файлы для изучения:**
- `handlers/tasks.py` (функция `mytasks_handler`)
- `database/models.py` (модель Task, связи с User и Chat)

---

### 📋 Баг #9: Текст задачи обрезается при создании через intent detection
**Приоритет:** Средний  
**Статус:** 📋 В очереди на анализ

**Описание проблемы:**
При создании задачи через автоматическое определение намерения (intent_router, без команды `/task`), текст задачи обрезается. Часть важной информации теряется. При этом команда `/detect` работает корректно и создает задачу с полным текстом.

**Пример воспроизведения:**
```
Пользователь: надо не забыть про воду
Бот: ✅ Готово, задача создана.
     📌 про воду  ❌ Обрезано "не забыть"
     👤 @murzagalin
     📅 Срок не указан

Пользователь: /detect
Бот: ✅ Готово, создал!
     📌 не забыть про воду  ✅ Правильно через /detect
     👤 @murzagalin
     📅 Срок не указан
```

**Ожидаемое поведение:**
- Текст задачи должен быть полным: "не забыть про воду"
- Не должна теряться важная информация из исходного сообщения
- Поведение должно быть одинаковым для intent_router и `/detect`

**Шаги для анализа:**
- [ ] Сравнить логику извлечения текста задачи в `handlers/intent_router.py` и `handlers/task_detector.py`
- [ ] Проверить, почему `/detect` работает правильно, а intent_router обрезает текст
- [ ] Изучить промпты для LLM в `llm/intent_prompts.py` (для intent_router)
- [ ] Проверить, как LLM извлекает текст задачи из сообщения в intent_router
- [ ] Проверить fallback логику в intent_router, если LLM не используется
- [ ] Изучить обработку в `handlers/task_detector.py` (работает правильно)
- [ ] Найти различия в логике между двумя подходами

**Файлы для изучения:**
- `handlers/intent_router.py` (извлечение данных из намерения)
- `handlers/intent_executors.py` (создание задачи)
- `llm/intent_prompts.py` (промпты для извлечения текста задачи)
- `handlers/task_detector.py` (если используется)

---

### 📋 Баг #10: Команда /setadmin перехватывается /task
**Приоритет:** 🔴 Критично  
**Статус:** 📋 В очереди на анализ

**Описание проблемы:**
Команда `/setadmin` (с упоминанием бота или без) перехватывается ConversationHandler для `/task`, из-за чего бот отвечает "А что делать-то надо? Напиши текст задачи." вместо выполнения команды.

**Пример воспроизведения:**
```
Пользователь: /setadmin@sanya_sanechka_bot
Бот: А что делать-то надо? Напиши текст задачи.  ❌ Неправильно
```

**Ожидаемое поведение:**
- `/setadmin` должен запросить username пользователя для назначения админом
- Не должен перехватываться ConversationHandler для `/task`

**Связь с другими багами:**
- Похожая проблема с багами #3 (`/summary`), #7 (`/subscribe`, `/reminders`)
- Общая проблема: ConversationHandler для `/task` слишком агрессивно перехватывает команды
- Все эти баги, вероятно, имеют одну общую причину - неправильный порядок обработчиков

**Шаги для анализа:**
- [ ] Изучить порядок регистрации обработчиков в `handlers/base.py`
- [ ] Проверить, почему ConversationHandler для `/task` перехватывает другие команды
- [ ] Проверить фильтры в ConversationHandler для `/task`
- [ ] Изучить обработчик `/setadmin` в `handlers/admin.py`
- [ ] Проверить, правильно ли зарегистрирован CommandHandler для `/setadmin`
- [ ] Убедиться, что CommandHandler регистрируются ДО ConversationHandler

**Файлы для изучения:**
- `handlers/base.py` (порядок регистрации обработчиков)
- `handlers/tasks.py` (ConversationHandler для /task, фильтры)
- `handlers/admin.py` (обработчик /setadmin)

---

## 🐛 Исправленные баги

## 🐛 Исправленные баги

### ✅ Критический: ConversationHandler блокирует пользователей
**Приоритет:** 🔴 Критичный  
**Статус:** ✅ Завершено

**Проблема:**
Пользователи не могли выйти из создания задачи:
- `/cancel` не работал
- `/cancel@botname` не работал
- Любые другие команды воспринимались как текст задачи

**Решение:**
- Добавлена проверка на команды в `receive_task_text()`, `receive_task_assignee()`, `receive_task_deadline()`
- При получении любой команды (начинается с `/`) происходит выход из ConversationHandler
- Упрощен `cancel_handler` - теперь всегда очищает данные и завершает диалог

**Файлы:**
- `handlers/tasks.py` (строки 755, 781, 946)
- `handlers/start.py` (строка 113)

---

### ✅ `/mytasks` не работает в ЛС
**Приоритет:** Средний  
**Статус:** ✅ Завершено

**Проблема:**
Команда `/mytasks` в личных сообщениях не отвечала

**Решение:**
- Добавлена обработка ошибок с try-except
- Проблема решилась после исправления ConversationHandler

**Файлы:**
- `handlers/tasks.py` (строка 1241)

---

### ✅ `/done` не работает после редактирования задачи
**Приоритет:** Высокий  
**Статус:** ✅ Завершено

**Проблема:**
После редактирования задачи через `/edit`, команда `/done` на сообщение "Задача изменена" не работала - бот отвечал "Это не задача"

**Решение:**
- При отправке подтверждения редактирования задачи теперь сохраняется новый `confirmation_message_id`
- Аналогично для напоминаний

**Файлы:**
- `handlers/tasks.py` - функции `_process_inline_edit()` и `_process_reminder_edit()`

---

### ✅ YandexGPT имитирует действия в ответах
**Приоритет:** Средний  
**Статус:** ✅ Завершено

**Проблема:**
YandexGPT генерировал имитацию процесса поиска:
```
Понятия не имею, погуглю за секунду. Гуглит Население Томмота — около 5 тысяч человек.
```

**Решение:**
Обновлен системный промпт в `SYSTEM_PROMPT`:
```
КАК ОТВЕЧАЕШЬ:
- Говоришь прямо, БЕЗ имитации действий
- НЕ пиши "гуглю", "ищу", "смотрю", "думаю" - сразу давай ответ
- Если знаешь - отвечаешь сразу
- Если не знаешь - говоришь честно, но без театра
```

**Файлы:**
- `llm/client.py` (строка 8)

---

## ✨ Реализованные фичи

### ✅ Умное закрытие задач через реплай
**Приоритет:** Высокий  
**Статус:** ✅ Завершено

**Описание:**
Пользователь может написать реплаем на задачу "закрой задачу" / "выполнено" / "done" → бот предлагает закрыть с подтверждением

**Функционал:**
1. Определение намерения "закрыть задачу" через LLM
2. Проверка прав (только автор или исполнитель)
3. Показ кнопки подтверждения
4. Закрытие задачи при подтверждении

**Ключевые слова:**
"закрой", "закрыть", "выполнено", "выполнил", "сделал", "done", "готово", "завершить"

**Файлы:**
- `utils/intent_helpers.py` - добавлен `IntentType.CLOSE_TASK`
- `llm/intent_prompts.py` - обновлен `REPLY_INTENT_PROMPT`
- `handlers/reply_analyzer.py` - добавлена поддержка CLOSE_TASK
- `handlers/ask.py` - добавлена функция `_handle_task_close_from_reply()`
- `handlers/tasks.py` - добавлены обработчики кнопок `close_confirm` и `close_cancel`

---

### ✅ Контекст последних сообщений при упоминании
**Приоритет:** Высокий  
**Статус:** ✅ Завершено

**Описание:**
Когда пользователь пишет код с ошибкой, а затем `@bot где ошибка?`, бот теперь видит предыдущие сообщения пользователя

**Функционал:**
- Собираются последние 5 сообщений пользователя из чата
- Контекст передается в LLM вместе с текущим вопросом
- Формат: "Контекст последних сообщений: ... Текущий вопрос: ..."

**Файлы:**
- `handlers/mention_handler.py` - функция `_get_user_message_context()`

---

## 📋 Запланированные улучшения

### 📋 Улучшение парсинга времени
**Приоритет:** Низкий  
**Статус:** 📋 Запланировано

**Описание:**
Добавить поддержку большего количества форматов времени:
- "через 2.5 часа"
- "в полдень", "в полночь"
- "через неделю"
- "в следующий вторник"

**Файлы:**
- `utils/date_parser.py`

---

### 📋 Групповые задачи
**Приоритет:** Низкий  
**Статус:** 📋 Запланировано

**Описание:**
Возможность назначить задачу нескольким исполнителям одновременно

**Требуется:**
- Изменение схемы БД (связь many-to-many)
- Обновление логики создания и редактирования задач
- Обновление форматирования сообщений

---

### 📋 Уведомления за N времени до дедлайна
**Приоритет:** Средний  
**Статус:** 📋 Запланировано

**Описание:**
Возможность настроить уведомление за определенное время до дедлайна (например, за час или за день)

**Требуется:**
- Добавление поля в модель Task
- Обновление scheduler для проверки предупредительных уведомлений
- UI для настройки времени уведомления

---

### 📋 Приоритеты задач
**Приоритет:** Низкий  
**Статус:** 📋 Запланировано

**Описание:**
Добавить возможность указывать приоритет задачи (высокий/средний/низкий) с визуальным индикатором

---

### 📋 Вложения к задачам
**Приоритет:** Низкий  
**Статус:** 📋 Запланировано

**Описание:**
Возможность прикрепить файл или изображение к задаче

**Требуется:**
- Хранение file_id в БД
- Обновление форматирования сообщений
- Логика для работы с вложениями

---

## 🔧 Технический долг

### 📋 Рефакторинг Intent Classification System
**Приоритет:** Низкий  
**Статус:** 📋 Запланировано

**Описание:**
- Унифицировать логику определения интентов
- Уменьшить дублирование кода
- Улучшить обработку ошибок

---

### 📋 Добавить unit-тесты
**Приоритет:** Средний  
**Статус:** 📋 Запланировано

**Охват:**
- Парсинг дат (`utils/date_parser.py`)
- Intent classification (`handlers/intent_router.py`)
- Fuzzy matching пользователей
- Форматирование сообщений

---

### 📋 Миграция на Alembic
**Приоритет:** Низкий  
**Статус:** 📋 Запланировано

**Описание:**
Использовать Alembic для управления миграциями базы данных вместо ручных SQL-скриптов

---

## 📝 Примечания

### История изменений
- **24.01.2026** - Исправлены все критические баги, добавлены фичи умного закрытия и контекста
- **23.01.2026** - Изменен стиль всех сообщений на "Толян" persona
- **22.01.2026** - Реализована система Intent Classification

### Текущие ограничения
- Лимит 10 вопросов к LLM в день на пользователя
- Задачи можно создавать только в чатах, где активен бот
- Timezone: Asia/Vladivostok (UTC+10)

### Известные проблемы
Нет известных критических проблем

---

**Последнее обновление:** 24 января 2026


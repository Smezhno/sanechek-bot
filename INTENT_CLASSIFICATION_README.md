# Intent Classification System

## Overview

Система автоматического определения намерений пользователя из обычных текстовых сообщений. Позволяет создавать задачи, напоминания и задавать вопросы боту без использования команд.

## Features

### ✅ Implemented

1. **Intent Detection** - Автоматическое определение намерений из текста
   - Задачи: "надо сделать", "нужно доработать"
   - Напоминания: "напомни через час", "завтра напомни"
   - Вопросы: "как это работает?", "что такое X?"

2. **Hybrid Classification** - Гибридный подход к классификации
   - Rules Engine: быстрые паттерны для 80%+ случаев
   - LLM Classifier: умная классификация для сложных случаев

3. **Smart Confirmation** - Умное подтверждение действий
   - Высокая уверенность + простое действие → выполнить автоматически
   - Низкая уверенность или сложное действие → показать подтверждение
   - Очень низкая уверенность → игнорировать

4. **Reply Intelligence** - Контекстно-зависимая обработка реплаев
   - На задачу → редактирование (дедлайн, исполнитель, текст)
   - На напоминание → изменение времени
   - На ответ бота → продолжение диалога

5. **Command Compatibility** - Полная совместимость с командами
   - Все команды `/task`, `/remind`, `/ask` работают как раньше
   - Intent Router обрабатывает только обычные сообщения

## Architecture

```
User Message
     ↓
[Commands?] → Existing Handlers
     ↓ no
[Intent Router]
     ↓
[Rules Engine] → Fast pattern matching
     ↓ unclear
[LLM Classifier] → Smart classification
     ↓
[Confidence Check]
     ↓
[Auto Execute / Confirm / Ignore]
     ↓
[Action Executors]
```

## Components

### 1. Intent Router (`handlers/intent_router.py`)
- Центральный модуль классификации
- Rules Engine для быстрой обработки
- LLM fallback для сложных случаев

### 2. Intent Helpers (`utils/intent_helpers.py`)
- Типы намерений (IntentType enum)
- Логика уверенности (confidence thresholds)
- Определение сложности действий

### 3. Intent Executors (`handlers/intent_executors.py`)
- Выполнение действий на основе классифицированных намерений
- Переиспользование существующей логики

### 4. Reply Analyzer (`handlers/reply_analyzer.py`)
- Анализ контекста реплая (задача/напоминание/ответ)
- LLM-based определение намерения реплая

### 5. LLM Prompts (`llm/intent_prompts.py`)
- Промпты для классификации намерений
- Промпты для анализа реплаев

## Usage Examples

### Creating Tasks

```
Пользователь: "надо купить молоко"
Бот: ✅ Задача создана: "купить молоко"

Пользователь: "Вася, можешь доработать функцию к пятнице?"
Бот: Кажется, ты хочешь создать задачу:
     "доработать функцию"
     Исполнитель: Вася
     Дедлайн: в пятницу
     
     Создать? [✅ Да] [❌ Нет]
```

### Creating Reminders

```
Пользователь: "напомни мне через час позвонить боссу"
Бот: ✅ Напомню в 15:30: "позвонить боссу"

Пользователь: "завтра в 9 утра встреча"
Бот: Кажется, ты хочешь напоминание:
     "встреча"
     Время: завтра в 9 утра
     
     Создать? [✅ Да] [❌ Нет]
```

### Asking Questions

```
Пользователь: "как работает эта функция?"
Бот: [Отвечает как Толян с объяснением]

Пользователь: "что такое рекурсия?"
Бот: [Даёт ответ]
```

### Smart Replies

```
Бот: "✅ Задача создана: купить молоко"
Пользователь: (реплай) "завтра до обеда"
Бот: ✏️ Задача изменена:
     🕐 Срок: завтра в 12:00

Бот: "✅ Напомню в 15:00: встреча с клиентом"
Пользователь: (реплай) "лучше в 16:00"
Бот: ✏️ Напоминание изменено:
     🕐 Новое время: сегодня в 16:00
```

## Configuration

### Confidence Thresholds

В `utils/intent_helpers.py`:

```python
CONFIDENCE_THRESHOLDS = {
    "auto_execute": 0.85,  # Автовыполнение
    "confirm": 0.65,       # Подтверждение
    "ignore": 0.65         # Игнорировать
}
```

### Rules Engine Patterns

В `handlers/intent_router.py`:

```python
# Паттерны задач
TASK_PATTERNS = [
    r'\b(надо|нужно|необходимо)\b',
    r'\b(сделать|доработать|исправить)\b',
    ...
]

# Паттерны напоминаний
REMINDER_PATTERNS = [
    r'\b(напомни|напомнить)\b',
    r'\bчерез\s+\d+\s+(минут|час)\b',
    ...
]
```

## Testing

### Run Unit Tests

```bash
python tests/test_intent_classification.py
```

### Test with LLM

```bash
python tests/test_intent_classification.py --llm
```

### Manual Testing

1. Отправь обычное сообщение в группу: "надо купить хлеб"
2. Бот должен создать задачу автоматически
3. Отправь: "Вася, сделай отчёт к пятнице"
4. Бот должен показать подтверждение с кнопками

## Performance Metrics

### Target Metrics

- ✅ 80%+ сообщений обрабатываются Rules Engine (без LLM)
- ✅ 90%+ точность определения намерений
- ✅ < 5% ложных срабатываний
- ✅ Команды работают без изменений
- ✅ Среднее время ответа < 2 секунды

### Optimization

1. **Rules Engine First** - 80% случаев обрабатываются без LLM
2. **Short Prompts** - Минимальная длина промптов (< 200 tokens)
3. **Timeout** - 5 секунд на LLM запрос
4. **Caching** - Опционально: кэширование похожих запросов

## Troubleshooting

### Bot Not Responding to Natural Language

1. Проверь что Intent Router добавлен в handlers: `handlers/base.py`
2. Проверь группу обработки: должна быть -1 (earliest)
3. Проверь логи: `journalctl -u sanechek-bot -f`

### Low Accuracy

1. Настрой пороги confidence в `utils/intent_helpers.py`
2. Добавь паттерны в Rules Engine
3. Улучши промпты в `llm/intent_prompts.py`

### Too Many Confirmations

1. Увеличь `auto_execute` threshold (например, до 0.80)
2. Добавь больше паттернов в Rules Engine для высокой уверенности

### LLM Timeouts

1. Уменьши `max_tokens` в вызовах ask_llm
2. Упрости промпты
3. Добавь больше правил в Rules Engine

## Future Enhancements

### Phase 2 (Optional)

- [ ] Expense detection: "потратил 500р на обед"
- [ ] Multi-turn conversations
- [ ] Context memory (remember recent topics)
- [ ] Batch operations: "создай 3 задачи: X, Y, Z"

### Phase 3 (Advanced)

- [ ] User preference learning
- [ ] Custom patterns per chat
- [ ] Analytics dashboard
- [ ] A/B testing for prompts

## Migration Notes

### From Commands to Natural Language

Существующие команды продолжают работать! Новая система добавляет альтернативный способ:

**Before:**
```
/task купить молоко @ivan завтра
```

**After (both work):**
```
/task купить молоко @ivan завтра
или
Иван, купи молоко до завтра
```

### Backward Compatibility

- ✅ Все существующие команды работают
- ✅ ConversationHandlers не затронуты
- ✅ Callback handlers не изменены
- ✅ База данных не изменена

## Support

Вопросы и баги: создай issue или напиши в чат.

